name: Release
on:
  workflow_call:

jobs:
  release:
    name: Release
    runs-on:
      - self-hosted
      - k8s-api
      - linux-amd64-2-4096
      - burstable
      - linux-amd64-s

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Release
        id: build
        run: |
          echo "This is a Release stage"
